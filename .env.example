# Copy to `.env` (gitignored) and fill in. Kamal loads this automatically.

# Rails credentials key — the contents of config/master.key.
RAILS_MASTER_KEY=

# ActiveRecord encryption keys (protect the stored MCP tokens).
# Generate fresh 32-char values: bin/rails runner 'puts SecureRandom.alphanumeric(32)'
AR_ENCRYPTION_PRIMARY_KEY=
AR_ENCRYPTION_DETERMINISTIC_KEY=
AR_ENCRYPTION_KEY_DERIVATION_SALT=
