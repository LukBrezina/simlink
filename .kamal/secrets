# Secrets for Kamal, read from your shell environment (Kamal auto-loads `.env`).
# The local registry (localhost:5555) is unauthenticated, so no registry password.
#
# Copy .env.example to .env, fill in real values, then run bin/kamal deploy.

RAILS_MASTER_KEY=$RAILS_MASTER_KEY
AR_ENCRYPTION_PRIMARY_KEY=$AR_ENCRYPTION_PRIMARY_KEY
AR_ENCRYPTION_DETERMINISTIC_KEY=$AR_ENCRYPTION_DETERMINISTIC_KEY
AR_ENCRYPTION_KEY_DERIVATION_SALT=$AR_ENCRYPTION_KEY_DERIVATION_SALT
